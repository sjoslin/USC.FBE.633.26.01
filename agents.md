# Agents - Manual Instructions

**DO NOT push to remote or commit to git** - these instructions are for your manual handling.

## What to do:

1. **Review the files** in this repository and make any necessary edits
2. **Commit changes manually** (outside of this agent session)
3. **Push to remote** manually when ready

## Files to review:

- `index.md` - Course landing page (overview + motivation + structure)
- `_config.yml` - Site configuration
- `week_0/` through `week_6/` - Weekly content
- `assets/images/` - Screenshots (openclaw1.jpeg, openclaw2.jpeg)

## Ready to push:

Once satisfied with local testing, run:

```bash
cd /workspace/USC.FBE.633.26.01
git add .
git commit -m "Update site content"
git push origin main
```

Then wait ~1-2 minutes for GitHub Pages to rebuild.
